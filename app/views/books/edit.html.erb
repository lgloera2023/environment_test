<nav><%= link_to("Home", books_path, :class => 'action index') %></nav>

<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>
    <%= render(:partial => 'form', :locals => {:f => f}) %>

    <%= f.submit("Save") %>
<% end %>

<%= link_to 'Show', @book %>
